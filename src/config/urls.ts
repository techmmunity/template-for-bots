export const urls = {
	// Discord
	discordServerSupportInvite: () => "",

	// Platform
	platformAccessContent: (storeId: string, productId: string) =>
		`https://app.maite.chat/contents/${storeId}/${productId}`,
};
